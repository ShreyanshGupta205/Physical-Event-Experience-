{
  "name": "smart-event-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "netstat -ano | find \":3000\" > nul && (for /f \"tokens=5\" %a in ('netstat -aon ^| findstr :3000') do taskkill /f /pid %a) || (exit /b 0)",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "lucide-react": "^0.417.0",
    "next": "^16.2.4",
    "qrcode.react": "^3.1.0",
    "react": "^18",
    "react-dom": "^18",
    "recharts": "^2.12.7"
  }
}
